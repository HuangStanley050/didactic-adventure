import ActionType from "../actions";

const initialState = {
  defaultAnimes: []
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
